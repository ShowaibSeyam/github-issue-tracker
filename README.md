    - 1️⃣ What is the difference between var, let, and const?
    =>  var is a old way to declare variable. If i declare variable via var then the variable can be redeclared and reassigned.
        let is used to give input and later user can update it but const is a constant variable and it can't be updated.


    - 2️⃣ What is the spread operator (...)?
    => spread operator is uded to copy an array or something like that.It also help to generate arrays from a given object.

    
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    => 

    map() used to transform each element of an array and returns a new array.

    filter() used to select element with a matching condition.

    foreach() used to iterate though an array to perform actions.


    - 4️⃣ What is an arrow function?
    => Arrow function (=>) is a shorter way to write a function in js. Like - 
       const add = (a,b)=>{
        return a+b;
       };



    - 5️⃣ What are template literals?
    =>  template literats are backticks (``). It is a way to write the html code into javascript file.
    





